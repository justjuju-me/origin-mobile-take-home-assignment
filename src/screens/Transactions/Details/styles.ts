import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  vendor: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 10,
  },
  aditionalInfo: {
    fontSize: 16,
    marginBottom: 10,
  },

  amount: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 10,
  },

  date: {
    fontSize: 16,
    marginBottom: 10,
  },

  map: {
    width: "50%",
  },

  receipt: {
    width: "50%",
    paddingBottom: 20,
  },
});

export default styles;
