import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },

  selfie: {
    width: 100,
    height: 100,
    marginLeft: "auto",
    marginRight: "auto",
    marginBottom: 10,
  },

  error: {
    textAlign: "center",
    color: "#de4b4b",
  },
});

export default styles;
