import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  error: {
    textAlign: "center",
    color: "#de4b4b",
  },
});

export default styles;
