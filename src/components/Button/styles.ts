import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderColor: "black",
    padding: 10,
    marginBottom: 10,
  },

  text: {
    textAlign: "center",
  },
});

export default styles;
