import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 200,
    height: 200,
    marginBottom: 10,
  },

  title: {
    fontSize: 20,
    marginBottom: 10,
  },
});

export default styles;
